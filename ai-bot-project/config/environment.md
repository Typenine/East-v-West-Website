# Environment Variables (v1)

Required later when we start coding:
- LEAGUE_ID = 1205237529570193408
- OUT_DIR = out

Optional (v1, only if we add AI phrasing later):
- OPENAI_API_KEY = (leave blank for now)

Testing helper:
- FORCE_WEEK = (number; overrides current week selection)
